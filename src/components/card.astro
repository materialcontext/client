---
const {over, under} = Astro.props;
---
<div class="flex">
    <img class="w-10 h-10 p-1 rounded-full" src="/logo00.svg" alt="profile pic">
    <div class="flex flex-col ml-1">
        <span class="info-dark text-[15px]">{over}</span>
        <span class="info-light text-[15px]">{under}</span>
    </div>
</div>
